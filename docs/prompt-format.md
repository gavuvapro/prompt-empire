# Prompt Format

Every prompt uses predictable headings so the website can index it reliably.

Required sections:

1. `# Prompt Title`
2. `## Description`
3. `## Best AI Models`
4. `## Use Cases`
5. `## Prompt`
6. `## Example Output`

Use placeholders with double braces, for example `{{audience}}` or `{{code}}`.
