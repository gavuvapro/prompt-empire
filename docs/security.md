# Security Notes

Prompt Empir treats prompt files as reviewed content. The website renders Markdown through a sanitizer and does not execute scripts embedded in prompt content.

When contributing, never include secrets, credentials, exploit instructions, or prompt injection payloads intended to compromise users.
